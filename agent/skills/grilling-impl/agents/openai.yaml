interface:
  display_name: "Grilling Impl"
  short_description: "Grill, implement, and independently verify"
  default_prompt: "Use $grilling-impl to resolve decisions, delegate implementation to the named implementer, then independently verify it with the named verifier."
