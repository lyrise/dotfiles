USERPROFILE=$(wslpath "$(wslvar USERPROFILE 2> /dev/null)")
ProgramData=$(wslpath "$(wslvar ProgramData 2> /dev/null)")
ProgramFiles=$(wslpath "$(wslvar ProgramFiles 2> /dev/null)")
GIT_INSTALL_ROOT=$(wslpath "$(wslvar GIT_INSTALL_ROOT 2> /dev/null)")

# VSCode
export PATH=$PATH:"$USERPROFILE/AppData/Local/Programs/Microsoft VS Code/bin"

# Docker
export PATH=$PATH:"$ProgramFiles/Docker/Docker/resources/bin"
export PATH=$PATH:"$ProgramData/DockerDesktop/version-bin"

# Git
function git () {
  if [[ `pwd -P` == /mnt/* ]]; then
    "$GIT_INSTALL_ROOT/bin/git.exe" "$@"
  else
    /usr/bin/git "$@"
  fi
}

# pwsh
function pwsh () {
  if [[ `pwd -P` == /mnt/* ]]; then
    "$USERPROFILE/scoop/shims/pwsh.exe" "$@"
  else
    /usr/bin/pwsh "$@"
  fi
}
